<tool id="Percenter" name="Normalize" version="1.0.0">
  <description>a BigWig file</description>
  <command interpreter="ruby1.9">percenter.rb -i $input -t $T -g $dbkey -p 4 -o $output</command>
  <inputs>
		<param format="bigwig" name="input" type="data" label="Normalize the data in" />
		<param name="T" type="integer" value="0" label="Divide by (leave 0 to divide by total coverage)" />
  </inputs>
  <outputs>
		<data format="input" name="output" metadata_source="input" />
  </outputs>
  
	<help>
		.. class:: warningmark
		
		This tool requires BigWig formatted data. If your data is in Wig format, use the Convert Formats -> Wig-to-BigWig tool.
		
		.. class:: warningmark
		
		This tool was contributed by Timothy Palpant.
	</help>
</tool>
