<tool id="WigAvg" name="Average" version="1.0.0">
  <description>multiple Wiggle files</description>
  <command interpreter="ruby1.9">
		averager.rb -g $dbkey -p 4 -o $output
			#for $input in $inputs
				${input.file}
			#end for
	</command>
  <inputs>
			<repeat name="inputs" title="BigWig files">
				<param name="file" type="data" format="bigwig" />
			</repeat>
  </inputs>
  <outputs>
      <data format="input" name="output" />
  </outputs>
  
<help>
.. class:: warningmark

This tool requires BigWig formatted data. If your data is in Wig format, use the Convert Formats -> Wig-to-BigWig tool.

.. class:: warningmark

This tool was contributed by Timothy Palpant.
</help>
</tool>
