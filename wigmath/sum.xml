<tool id="WigSum" name="Add" version="1.0.0">
  <description>multiple Big files</description>
  <command interpreter="ruby1.9">
		sum.rb -g $dbkey -p 4 -o $output
			#for $input in $inputs
				${input.file}
			#end for
	</command>
  <inputs>
			<repeat name="inputs" title="Wig file">
				<param name="file" type="data" format="bigwig" />
			</repeat>
  </inputs>
  <outputs>
      <data format="input" name="output" />
  </outputs>
  
<help>
.. class:: warningmark

This tool requires BigWig formatted data. If your data is in Wig format, use the Convert Formats -> Wig-to-BigWig tool.

.. class:: warningmark

This tool was contributed by Timothy Palpant.
</help>
</tool>
