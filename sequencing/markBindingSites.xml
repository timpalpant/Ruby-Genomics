<tool id="MarkBindingSites" name="Mark binding sites" version="1.0.0">
  <description>relative to alignment loci</description>
  <command interpreter="ruby1.9">markBindingSites.rb -i $input -l $loci -o $output</command>
  <inputs>
      <param name="input" type="data" format="tabular" label="Binding sites (MACS peak calls file)" />
			<param name="loci" type="data" format="bed" label="Loci to align to" />
  </inputs>
  <outputs>
      <data name="output" format="tabular" />
  </outputs>
  
<help>
.. class:: warningmark

This tool requires peak calls in MACS format and alignment loci in Bed format, where the 5th column is the base pair to align to.

.. class:: warningmark

This tool was contributed by Timothy Palpant.
</help>
</tool>
